import type { NextPage } from "next";

import MapComponent from "../app/components/map";
const Home: NextPage = () => {
  return (
    <div>
      <MapComponent />
    </div>
  );
};

export default Home;
